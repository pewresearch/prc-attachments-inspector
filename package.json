{
	"name": "@prc/attachments-inspector",
	"version": "1.0.1",
	"description": "PRC Attachments Inspector is a module for PRC Platform that...",
	"scripts": {
		"build": "wp-scripts build --source-path=includes/attachments-panel/src --output-path=includes/attachments-panel/build && wp-scripts build --source-path=includes/attachment-report/src --output-path=includes/attachment-report/build && wp-scripts build --source-path=includes/image-mismatch/src --output-path=includes/image-mismatch/build"
	},
	"devDependencies": {
		"@wordpress/scripts": "^31.1.0"
	},
	"dependencies": {
		"@prc/hooks": "*",
		"@wordpress/icons": "^10.21.0"
	},
	"license": "GPL-2.0-or-later"
}