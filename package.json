{
	"name": "@prc/attachments-inspector",
	"version": "1.0.0",
	"description": "PRC Attachments Inspector is a module for PRC Platform that...",
	"scripts": {
		"build": "wp-scripts build --source-path=includes/attachments-panel/src --output-path=includes/attachments-panel/build; wp-scripts build --source-path=includes/attachment-report/src --output-path=includes/attachment-report/build",
		"test": "wp-scripts test-playwright",
		"test:clean": "wp-env clean all && wp-env start && wp-scripts test-playwright",
		"test:env:start": "wp-env start",
		"test:env:stop": "wp-env stop",
		"test:env:clean": "wp-env clean all && wp-env start",
		"test:env:destroy": "wp-env destroy"
	},
	"devDependencies": {
		"@playwright/test": "^1.57.0",
		"@wordpress/e2e-test-utils-playwright": "^1.36.0",
		"@wordpress/env": "^10.36.0",
		"@wordpress/scripts": "^31.1.0"
	},
	"dependencies": {
		"@wordpress/icons": "^10.21.0"
	},
	"license": "GPL-2.0-or-later"
}
